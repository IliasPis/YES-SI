using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OpenActors : MonoBehaviour
{
    public GameObject OpenActorsObject;
    // Start is called before the first frame update
    void Start()
    {
        OpenActorsObject.SetActive(true);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
